import { Reset } from "styled-reset";
import { MainRouters } from "./routers/router";

export const App = () => {
  return (
    <>
      <Reset />
      <MainRouters />
    </>
  );
};

export default App;
